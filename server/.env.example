GROQ_API_KEY=

# Shared secret for future admin API features (outbound email disabled in app for now).
# ADMIN_API_SECRET=

# Optional: override Realtime Database URL if server code reads Firebase again later.
# FIREBASE_DATABASE_URL=
